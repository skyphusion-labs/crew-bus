{
  "name": "crew-bus-worker",
  "version": "0.7.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 execute crew-bus --local --file=schema.sql",
    "db:migrate:remote": "wrangler d1 execute crew-bus --remote --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260809.1",
    "@vitest/coverage-istanbul": "^4.1.11",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.120.0"
  }
}
